import React from 'react'

function FragmentDemo() {
    return (
        <React.Fragment>
            <h1>Fragment Demo</h1>
            <p>This is Fragment Demo</p>
        </React.Fragment>
    )
}

export default FragmentDemo

// Fragments let you group the children elements without adding extra nodes to the DOM.